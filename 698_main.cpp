#include <iostream>
#include <vector>
#include <istream>

using namespace std;

class Solution {
public:
    bool canPartitionKSubsets(vector<int>& nums, int k) {
        
             
        
    }
};



int main(){
	vector<int> v = {1,2,0,0,3,0,0};

	for(int i=0;i<v.size();i++)
		cout<<v[i]<<' ';

	return 0;
}